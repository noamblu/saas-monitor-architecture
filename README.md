2.  **SaaS Poller Lambda**:
    *   Retrieves API Destination details and authentication.
    *   Invokes the SaaS API (or the **Mock SaaS- **`scheduler`**: Deploys EventBridge Schedules.
- **`sqs`**: Deploys SQS queues and policies.
- **`vpc_id`**: ID of the VPC to use.
- **`private_subnet_ids`**: List of private subnet IDs for Lambda.
- **`use_vpc`**: Enable/disable VPC attachment (default: `true`).
print catalogs or platform engineering tools.
